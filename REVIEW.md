# REVIEW.md — ผลตรวจงาน colorwatch

เกณฑ์อ้างอิง: `COLORWATCH_PROMPT.md`
Reviewer: Claude (session รีวิว) / Implementer: Claude อีก session — **ไฟล์นี้คือช่องทางสื่อสารระหว่างกัน**

---

# รอบ 3 (2026-06-15) — สั่งงานใหม่ ถึง implementer 📋

> reviewer ไม่แตะโค้ดเอง — ข้างล่างนี้คือ scope ที่ผู้ใช้อนุมัติให้ทำรอบนี้
> แผนเต็ม: `/Users/panda/.claude/plans/mac-sequential-blanket.md` (อ่านก่อนเริ่ม)

## หลักที่ต้องยึดตลอด (ผู้ใช้ย้ำ)
- **คนใช้หน้างานไม่ใช่ engineer** → UI ต้องกดง่าย เห็นภาพ ไม่ต้องพิมพ์คำสั่ง/รู้ camera index
- **แก้ให้น้อยที่สุด** ทำเป็นส่วนๆ อ่านง่าย (ยิ่งแก้เยอะยิ่งเสี่ยงพังตอนไปเทสหน้างาน)
- **รองรับข้ามแพลตฟอร์ม** Windows/Linux/mac — อย่าผูก mac โดยไม่จำเป็น

## ทำแค่ 2 ธีมนี้ (ที่เหลือเลื่อน — ห้ามทำเกิน)

### ธีม A — Detection เสถียร
- **A1** ยึดตำแหน่ง x ของดวงจาก "เฟรมที่เจอครบ N ครั้งแรก/ตอน calibrate" เป็นสล็อต
  แล้วเฟรมถัดไปจับ blob เข้าสล็อตใกล้สุด — สล็อตว่าง = ดวงนั้นหาย (รายงานชัด ไม่ error ทั้งจอ)
  คงการ map ดวง↔ชิ้นให้ถูกเสมอแม้บางดวงหาย (`multiwatch.py` update/find_spots, `webapp.py` แสดงผล)
- **A2** ผ่อนเกณฑ์ detection ให้ครอบดวงหรี่ได้ (ใช้คู่กับล็อกแสงพื้นดำ) — ไม่ทำ geometry engine เต็มรูป
- **A3** ล็อกแสงต่ำเป็น default กดง่าย + **แยก `set_exposure` เป็นชั้น cross-platform**:
  mac=uvc-util เดิม / Windows-Linux=OpenCV property (`CAP_PROP_AUTO_EXPOSURE`+`CAP_PROP_EXPOSURE`);
  ตั้งไม่ได้ต้องไม่ crash แค่แจ้งแล้วใช้ auto

### ธีม D — Device/Deploy
- **D10** camera picker เห็นภาพ: `/api/cameras` ไล่ OpenCV index 0–5 ส่ง thumbnail → ผู้ใช้คลิกตัวที่เห็น LED → จำ index ไว้ (เสริมชื่อจาก `uvc-util -d` ได้ แต่ภาพคือตัวตัดสิน)
- **D11** ปล่อยกล้องเมื่อไม่มีคนดู stream + ไม่ได้ตรวจ >~60 วิ แล้วเปิดคืนอัตโนมัติเมื่อขอ stream
- **D12** เอกสารใช้งานภาษาไทยฉบับคนทั่วไป + วิธีติดตั้ง/รันแยกตาม OS

## เลื่อนไว้ (ห้ามทำรอบนี้)
ธีม B (SNR: calibrate บังคับ/smooth ยาว/วัด noise→ลด thresh), ธีม C (อัดคลิป replay/preview สด/slider), กล้อง IP

## reviewer จะตรวจอะไร (acceptance)
- A1/A2: บัง/หรี่ 1 ดวง → วัดดวงที่เหลือได้ + ชี้ดวงที่ขาดถูกตำแหน่ง, map ชิ้นไม่เพี้ยน, ไม่ blank ทั้งจอ
- A3: บน mac คง uvc-util เดิม; path OpenCV property ตั้งไม่ได้ต้องไม่ crash
- D10: index สลับแล้วยังคลิกเลือกกล้อง LED ถูก
- D11: ทิ้งไว้ไม่ดู >60 วิ กล้องปล่อย, เปิดหน้าใหม่กลับมาเอง
- เกณฑ์ห้ามแตะเดิม: `--thresh 4.0`, `--hold 5.0` (รอบนี้ไม่อยู่ใน scope ที่ให้แก้)

## ผลตรวจรอบ 3 (reviewer, 2026-06-15) — **ผ่าน** (เทสต์บนโต๊ะ เหลือเทสต์หน้างาน)

ตรวจ diff: `multiwatch.py` (+5KB), `webapp.py` (+11KB), สร้าง `README_USE.md`/`requirements.txt`/`run_windows.bat`; `colorwatch.py` ไม่แตะ (ถูกต้อง นอก scope)

| จุด | ผลตรวจ |
|---|---|
| **A1** สล็อต + MISSING | ✅ เทสต์เฟรมจำลอง 4 ดวง: เจอครบ→ยึดสล็อต; ขาด 1 ดวง→MISSING เฉพาะช่องนั้น ดวงอื่นวัดต่อ ไม่ error ทั้งจอ; **ดวงหายแล้ว map ไม่เลื่อน** (ดวงฟ้ายังถูกฟันที่ช่องเดิม) — แก้บั๊กเดิมได้ตรงจุด |
| **A2** relax 0.85→0.6 | ✅ แยก `detect_blobs`/`find_spots` สะอาด, comment ผูกกับเงื่อนไข "พื้นดำ" ชัด |
| **A3** exposure cross-platform | ✅ mac→uvc-util เดิม, Win/Linux→OpenCV property; cap=None/กล้องตั้งไม่ได้ → คืนข้อความ ไม่ crash (เทสต์แล้ว); ฉลาดที่สั่ง OpenCV ใน capture thread กัน race กับ `read()` |
| **D10** camera picker | ✅ `/api/cameras` ส่ง thumbnail, คลิกเลือก, จำลง `config.json` — ตรงเป้า user ไม่ต้องรู้ index (ยังไม่ได้เทสต์กับหลายกล้องจริง) |
| **D11** ปล่อยกล้อง idle | ✅ logic ครบ (idle>60วิ + ไม่ตรวจ → break + release; lazy restart) — ยังไม่ได้เทสต์เวลาจริง |
| **D12** เอกสาร | ✅ README_USE.md user-friendly มาก, แยกติดตั้งตาม OS, ไฟล์ที่อ้างมีครบ |

syntax ผ่านทั้ง 3 ไฟล์, import webapp ได้, endpoints ครบ 9 ตัว

### ข้อสังเกต (ไม่ใช่ blocker — ฝากพิจารณา/ทำรอบหน้า)
1. **สล็อตยึดครั้งเดียว ไม่ self-correct** — โค้ดเก่า re-detect ทุก 10 วิ ของใหม่ยึดสล็อตครั้งแรกแล้วไม่ขยับอีก
   ถ้า (ก) เฟรมแรกล็อกพลาด (จับเงาสะท้อนเป็นดวง) หรือ (ข) กล้อง/แผงขยับระหว่างใช้ → ทุกช่องจะกลายเป็น MISSING
   จนกว่าจะ กดหยุด→เริ่มตรวจใหม่ หรือ calibrate ใหม่ (recovery มีแต่ผู้ใช้ต้องรู้)
   **เสนอ:** ถ้า MISSING ทุกช่องติดต่อกันสักพัก ให้ auto-เคลียร์สล็อตแล้ว re-detect เอง (กันงงหน้างาน)
2. **DEFAULT_EXPOSURE=5 auto-lock** — ดีสำหรับ C200/mac; บน Windows ที่ล็อกแสงไม่ได้ + ห้องสว่าง
   เกณฑ์ 0.6 อาจจับมั่ว README บอกให้จัดห้องมืดแล้ว — โอเค แต่ต้องเน้นย้ำตอนเทรนผู้ใช้

### ยังต้องเทสต์หน้างาน (reviewer ทำไม่ได้ — ห้องสว่าง + ห้ามเปิดกล้องค้าง)
- D10 เลือกกล้องจริงตอนมีหลายตัว / index สลับ
- D11 จับเวลาปล่อยกล้องจริง
- เทียบสีจริงในที่มืด + ยืนยันทิศ `--flip` (ค้างจากรอบ 2)

---

# รอบ 2 (2026-06-12 บ่าย) — ตรวจหลัง implementer แก้ตามรอบ 1

## คำตัดสินรวม: **ผ่าน** (เหลืองานที่ต้องให้ผู้ใช้ทำเอง 2 ข้อ ดูท้ายไฟล์)

## ผลตรวจ fix แต่ละจุดจากรอบ 1

| จุด | วิธีที่ implementer แก้ | ผลตรวจ |
|---|---|---|
| B1 วัดไม่ได้แล้วเงียบ | เพิ่ม error report ทุก interval (text+JSON พร้อม n_left/n_right), รีเซ็ต hold timer, pump หน้าต่าง --show | ✅ ผ่าน — การรีเซ็ต hold ตอนข้อมูลขาดถูกต้องตามเจตนา "ค้างต่อเนื่อง" |
| R1 ขอบไม้ bias ค่า diff | จำกัดการวัดด้วย**วงรี inscribed** ในกรอบ ROI (ใน `measure_bstar`) | ✅ ผ่าน — พิสูจน์กับ `raw_cam0.png`: R b* +0.71 → −0.36 (bias ~1 หน่วยหายตามที่ comment อ้าง), แต้มสีนอกวงรีไม่รั่วเข้าการวัด (ทดสอบยืนยันแล้ว) |
| R2 ไม่ล้าง state | `diffs.clear()` เมื่อ ROI เปลี่ยนตอน re-detect | ✅ ผ่าน (เคลียร์แม้ขยับ 1px — ยอมรับได้ window เติมคืนใน ~0.5 วิ) |
| R3 ไม่มี sanity check | เช็ค center สอง blob ต้องห่างแนวนอน ≥ 60% ของ blob กว้างสุด | ✅ ผ่าน — ทดสอบเคสก้อนใหญ่+เศษสะท้อนแกน x ใกล้กัน → ปฏิเสธถูกต้อง |
| เอกสารไม่ตรงโค้ด | อัปเดต 60%→85% ใน COLORWATCH_PROMPT.md สองจุด | ✅ ผ่าน — reviewer เติม JSON schema (+cct_k) กับ "สถานะปัจจุบัน" ให้ครบแล้ว |

## ของใหม่ที่ implementer เพิ่ม: `estimate_cct` (เคลวิน McCamy)

- ตรวจสูตรแล้วถูกต้อง (sRGB→linear→XYZ→xy→McCamy, median ต่อ pixel, มี guard หาร 0)
- เป็น **ข้อมูลประกอบเท่านั้น** ไม่ถูกใช้ตัดสิน BAD/OK — การตัดสินยังใช้ b* ตามสเปกเป๊ะ ✅
- คำนวณเฉพาะตอนรายงาน (1/วิ) ไม่กินทุกเฟรม ✅ ค่า live สมเหตุสมผล (~7000–7900K)
- หมายเหตุ: เกินสเปกข้อ 4 ("ต่อยอดถ้าผู้ใช้ขอ") เล็กน้อย แต่ผู้ใช้อนุมัติแบบเหมารวมแล้ว
  และไม่กระทบ logic ตัดสิน → ยอมรับ ไม่ต้อง revert

## บั๊กที่ reviewer พบในโค้ดรอบใหม่ (แก้แล้วโดย reviewer — ผู้ใช้อนุมัติ)

> ⚠️ **ถึง implementer**: reviewer แตะโค้ด 2 จุดนี้โดยตรง อย่าแปลกใจกับ diff

1. **`measure_bstar` relax branch ทำวงรีหลุด** — ตอน pixel < 50 แล้วผ่อนเกณฑ์เหลือ 40
   บรรทัดนั้นคำนวณ `usable` ใหม่โดยไม่ AND วงรีซ้ำ → การกันขอบแผงหายไป**เฉพาะเคสมืด
   ซึ่งคือเคสใช้งานจริง** แก้โดย factor `inside` ออกมาแล้ว AND ทั้งสอง branch
   (พิสูจน์: เคสมืด n_วงรี=2121 vs n_ไม่มีวงรี=2601, แต้มสีนอกวงรี → b* ไม่ขยับเลย)
2. **branch "หาไฟ 2 จุดไม่เจอ" ไม่ pump `--show`** — หน้าต่างค้าง/beachball บน macOS
   ถ้าหา blob ไม่เจอนานๆ แก้แบบเดียวกับ branch วัดไม่ได้ (imshow + waitKey + q ออกได้)

## ผลทดสอบรอบ 2 (ทั้งหมดผ่าน)

- **เฟรมจำลอง** ฟ้าซ้าย/ขาวขวา: diff = −13.5 ตรวจถูกฝั่ง
- **เคสมืด** (relax branch): วงรียังทำงาน, pixel นอกวงรีไม่รั่ว
- **sanity check**: ปฏิเสธคู่ blob ที่ไม่แยกซ้าย/ขวา
- **ภาพจริง** `raw_cam0.png`: L=−9.03 / R=−0.36, diff=−8.67
- **กล้องจริง cam 0 รัน 14 วิ (`run --json`)**: diff นิ่ง −5.1…−6.0,
  **hold trigger ตรงเวลา** (เริ่ม 955.7 → BAD แรก 960.9 = 5.2 วิ), ซ้ายถูกฟันธง BAD
  สอดคล้อง CCT (ซ้าย ~7800K เย็นกว่าขวา ~7000K) — snapshot ล่าสุด `snap_cam0.png` ROI ครอบครบ
- เกณฑ์ห้ามแตะยังคงเดิม: `--thresh 4.0`, `--hold 5.0` ✅

## งานที่เหลือ — ต้องให้ผู้ใช้ทำ (โค้ดพร้อมแล้ว)

1. **ทดสอบในสภาพแสงจริงที่มืด** — ภาพทดสอบทั้งหมดยังเป็นห้องสว่าง (gray max 253)
   รัน `python snapshot.py --cam 0` ในที่มืดจริงแล้วดูว่ายังเจอ 2 จุด / diff ไม่แกว่งเกิน ±4
   ตอนไฟปกติ (ถ้าแกว่ง: เพิ่ม `--smooth`/`--thresh` — **ห้ามแตะ hold 5 วิ**)
2. **ยืนยันทิศทาง** — เช็คว่า "ซ้าย" ใน output ตรงซ้ายจริง ไม่ตรงใช้ `--flip`
   (ตอนนี้ระบบฟันธง "ซ้าย" เพี้ยน — ผู้ใช้ช่วยยืนยันว่าตรงกับตาเห็น)

---

# รอบ 1 (2026-06-12 เช้า) — เก็บไว้เป็นประวัติ

ตรวจ `colorwatch.py` + `snapshot.py` เทียบสเปก: pipeline 6 ขั้นตรงครบ, CLI/JSON ตรง,
`--flip` trace ถูกต้อง, เกณฑ์ห้ามแตะไม่ถูกแก้, มีหลักฐานทดสอบกล้องจริง (cam0=แผง LED,
cam1 เปิดไม่ติด, cam2=กล้อง MacBook)

ประเด็นที่สั่งแก้ (ทั้งหมดถูกแก้แล้วในรอบ 2):
- **B1**: `measure_bstar` คืน None แล้วโปรแกรมเงียบสนิท (ไม่มี output/ไม่ pump --show)
- **R1**: ROI ขวาหลัง grow กินขอบไม้สีครีม (n=166,963 ใหญ่กว่าซ้ายมาก) → b* ขวาโดนดึงขึ้น
  เสี่ยง false positive ฝั่งซ้าย
- **R2**: ไม่ล้าง `diffs` ตอน re-detect / hold timer นับรวมช่วงข้อมูลขาด
- **R3**: เลือก blob ใหญ่สุด 2 อันโดยไม่เช็คว่าแยกซ้าย/ขวาจริง
- เอกสาร 60% ไม่ตรงโค้ด 85%
- การจูน 60%→85%: มีบันทึกเหตุผลครบตามกติกา แต่ทิศสวนกับที่สเปกคาดสำหรับที่มืด
  → ต้องพิสูจน์ด้วยการทดสอบที่มืดจริง (ยังค้าง — งานผู้ใช้ข้อ 1)
