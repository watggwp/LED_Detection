# HANDOFF — รอบที่ 10 (ผลเทสกล้องจริง: สีจริง + ลำเอียงตำแหน่ง → per-spot เลื่อนไปหลังเทสหน้างาน)

**วันที่:** 2026-06-15 — เทสกับกล้อง+ชิ้นงานจริง (ดวงดี+ดวงฟ้าจาง)

## ผลพิสูจน์ด้วยการสลับดวงจริง (ชี้ขาด)
```
ก่อนสลับ:  ซ้าย(ดี) +0.9  | ขวา(ฟ้า) -2.0
หลังสลับ:  ซ้าย -3.0 BAD  | ขวา +0.08 OK    ← เครื่องหมายวิ่งตาม "ดวง"
```
- **ความฟ้า ~3 หน่วย b* เป็นสีจริง** กล้องวัดได้ (ไม่ใช่ artifact) — โหมด absolute thresh 2.5 จับ NG ได้
- **แต่มีลำเอียงตำแหน่ง ~1 หน่วย** (ช่องซ้ายอ่านอุ่นกว่าขวาเสมอ): ดวงฟ้า@ซ้าย -3.0 / @ขวา -2.0, ดวงดี@ซ้าย +0.9 / @ขวา ~0
- ผลข้างเคียง: เทียบ ดวงดี@ซ้าย (\|dev\| ถึง 1.36) กับ ดวงฟ้า@ขวา (\|dev\| ลงแค่ 1.46) **เกือบทับกัน** → margin บาง เด้ง/หลุดได้ที่ thresh 2.5 เมื่อดวงอยู่คนละช่อง

## ค่าที่วัดได้ (อ้างอิง)
- ดวงดี: \|dev\| 0.72–1.36 (เฉลี่ย 1.12) | ดวงฟ้า: \|dev\| 1.46–2.42 (เฉลี่ย 1.88) — เทียบข้ามตำแหน่ง
- color signal จริง (เทียบช่องเดียวกัน) ~2.5–3 หน่วย ชัดเจน

## ทางแก้ที่ถูกจุด: per-spot reference (= M3 เดิม) — **เลื่อนตามที่ผู้ใช้สั่ง**
ให้แต่ละช่องจำขาวดีของตัวเอง → ลบลำเอียงตำแหน่ง → ดวงดี ~0 ทุกช่อง, ดวงฟ้าเด่นเท่ากันทุกช่อง, ตั้ง thresh ~1.5 ได้ปลอดภัย
**ผู้ใช้ตัดสินใจ: ยังไม่ทำตอนนี้** — "สุดท้ายยังไงก็ต้องมีค่า stable ไว้เทียบ" → ไปเทสหน้างานก่อน ค่อยพิจารณา per-spot

## สถานะที่ commit ไว้
- thresh ใช้งาน = **2.5** (จำใน config.json — แต่ config.json ถูก gitignore ไว้ เป็น state ต่อเครื่อง)
- ค้างหน้างาน: เทียบสีจริงในที่มืด, ยืนยันทิศ --flip, ลองหลายชิ้นยืนยัน thresh, + พิจารณา per-spot

---

# HANDOFF — รอบที่ 9 (เทสกล้องจริง → thresh ปรับได้ + จำถาวร)

**วันที่:** 2026-06-15
**ที่มา:** เทสหน้างานกับกล้องจริง พบว่า**ของจริงเพี้ยนฟ้าแบบจางๆ ห่างกันแค่ ~3-5 หน่วย b***
ซึ่ง**ต่ำกว่าเกณฑ์เดิม 4.0 → ไม่เคยขึ้น NG เลย** ผู้ใช้ขอให้ลดเกณฑ์เพื่อจับฟ้าจางๆ ได้
(ก่อนหน้านี้ thresh 4.0 อยู่ใน "ห้ามแตะ" — **ผู้ใช้อนุมัติเองรอบนี้**)

## บทเรียนจากหน้างาน (สำคัญ)
- **calibrate 2 ดวงพร้อมกัน = ค่ากลางถูกเฉลี่ย** → ดวงที่ฟ้ากว่าโชว์ dev แค่ครึ่งของส่วนต่างจริง
  (เคสจริง: ต่างกัน ~3 หน่วย แต่ absolute โชว์แค่ -2.2 / relative โชว์เต็ม ~-3.4)
- **relative mode โชว์ส่วนต่างเต็มกว่า** (เทียบ L↔R ตรงๆ) แต่จับได้เฉพาะ "ดวงที่ฟ้ากว่าอีกดวง"
  ถ้าทั้งคู่ฟ้าพร้อมกันต้องใช้ Calibrate ขาว (เทียบดวงดีภายนอก)

## การเปลี่ยนแปลง (`webapp.py`)
- เพิ่ม flag **`--thresh`** (default = None → อ่านจาก `config.json`, ถ้าไม่มีใช้ 4.0)
  **ค่าคงที่ `THRESH` ในโค้ดยังเป็น 4.0** (สเปกเดิมไม่ถูกแก้ — 2.5 เป็น opt-in ของผู้ใช้)
- **จำ thresh ถาวรใน `config.json`** (คู่กับ cam_index) เมื่อใส่ `--thresh` ครั้งหนึ่ง →
  เปิดเปล่าๆ ครั้งหน้าโหลดค่าที่จำไว้ (ดับเบิลคลิก .bat / `webapp.py` เปล่าก็ได้ 2.5)
- `api_start` ส่ง `thresh=THRESH` เข้า `MultiWatch`
- เพิ่ม field `thresh` ใน `/api/status` (preview+running) ให้ UI/debug เห็นค่าที่ใช้อยู่

## ตั้งค่าปัจจุบัน: **thresh = 2.5** (จูนจากของจริง)

## การทดสอบ
- unit: เปิดแบบไม่ใส่ flag → โหลด thresh 2.5 จาก config ✅ / config ไม่มี → default 4.0 ✅
- รีสตาร์ทแบบไม่ใส่ flag → `/api/status` คืน `thresh:2.5` ✅ (จำถาวรจริง)
- **เทสกล้องจริง (relative):** ดวงซ้ายฟ้า (dev ~-4.99) → **NG ที่ 6 วินาที** (= hold 5 วิ + จังหวะอ่าน)
  ดวงขวา OK — thresh 2.5 จับฟ้าจางได้เร็ว ตรงเป้า "เทสแต่ละครั้งต้องไว"

## ค้าง/ฝาก reviewer
- thresh 2.5 มาจากจูนหน้างานครั้งเดียว — ควรเก็บตัวอย่างชิ้นดี/ชิ้นเสียหลายๆ ตัวยืนยันว่า
  2.5 แยกดี-เสียได้ไม่ false (ดวงดี dev ต้อง < 2.5 เสมอ) — ยังเป็นงานหน้างาน
- ยังไม่ทำ UI ปรับ thresh (ตอนนี้ปรับผ่าน `--thresh` แล้วจำเอง) — ถ้าต้องการ slider หน้าเว็บค่อยยกรอบหน้า

---

# HANDOFF — รอบที่ 8 (ตอบ review รอบ 3: ผ่าน + เก็บ 2 ข้อสังเกต)

**วันที่:** 2026-06-15
**ที่มา:** REVIEW รอบ 3 ตัดสิน **ผ่านทั้ง 6 ข้อ** (A1–A3, D10–D12) ไม่มี blocker
มีข้อสังเกต non-blocker 2 ข้อ — จัดการตามนี้ (ไม่เงียบข้ามตามกติกา):

### ข้อสังเกต #1 (สล็อตยึดครั้งเดียว ไม่ self-correct) → **แก้แล้ว** (`multiwatch.py`)
- เพิ่ม `SLOT_RECOVER_SEC = 3.0` + `self.all_missing_since`
- ใน `update()`: ถ้า **ทุกช่อง MISSING ต่อเนื่องเกิน 3 วิ** → เคลียร์สล็อต (`slots=None`)
  เฟรมถัดไป `find_spots` ใหม่อัตโนมัติ: เจอครบ→ยึดสล็อตใหม่ / ไม่ครบ→ขึ้น "จัดวางให้ครบ"
- รีเซ็ต `all_missing_since` ใน `_lock_slots()` และเมื่อมีอย่างน้อย 1 ช่องเจอ
- **ทำไมปลอดภัย:** trigger เฉพาะตอน *ทุก* ช่องหาย (ดวงดับ/แผงหลุดเฟรม/ล็อกผิดที่แล้วดวงจริงอยู่อื่น)
  ขาดแค่บางดวงไม่ trigger — สล็อตเดิมคงอยู่ map ไม่เลื่อน
- **ทดสอบ (offline ผ่านหมด):**
  - R1: ยึดสล็อต→ป้อนเฟรมว่าง→หลัง 3 วิ สล็อตเคลียร์ แล้วขึ้น "เจอ 0/4 จัดวางให้ครบ"
  - R2: เฟรมแรกล็อกผิดที่ (200/400) → ดวงจริงโผล่ที่ 820/1020 → เคลียร์แล้ว **re-lock ตำแหน่งจริง**
  - R3 (regression): ขาดแค่ 1 ดวง → **ไม่เคลียร์**, map ไม่เลื่อน (ดวง4 ยัง i=4)
  - R4 (regression): A2 rel 0.6 ยังเจอดวงหรี่ 4/4

### ข้อสังเกต #2 (DEFAULT_EXPOSURE=5 + Windows ห้องสว่างอาจจับมั่ว) → **เสริมเอกสาร** (`README_USE.md`)
- ไม่ใช่บั๊กโค้ด (ตั้งใจ auto-lock + best-effort) — เพิ่มคำเตือน ⚠️ เน้นย้ำว่าบน Win/Linux
  ที่ล็อกแสงไม่ได้ **ต้องจัดพื้นหลังให้มืดก่อนเริ่มตรวจเสมอ** (mac ที่ล็อกได้ไม่ต้องห่วง)

ไม่แตะ `--thresh`/`--hold`, ไม่แตะ `REVIEW.md`

### ค้างเทสหน้างาน (ตามที่ reviewer ระบุ — ทำแทนไม่ได้)
D10 หลายกล้องจริง/index สลับ, D11 จับเวลาปล่อยกล้องจริง, เทียบสีจริงในที่มืด + ยืนยันทิศ `--flip`

---

# HANDOFF — รอบที่ 7 (Detection เสถียร + Device/Deploy ตาม REVIEW รอบ 3)

**วันที่:** 2026-06-15
**ขอบเขต:** ทำตาม `REVIEW.md` รอบ 3 + แผน `~/.claude/plans/mac-sequential-blanket.md`
เฉพาะ **ธีม A (A1–A3)** + **ธีม D (D10–D12)** — ธีม B/C และกล้อง IP เลื่อนตามสั่ง ไม่แตะ
**ยึดหลัก:** UI กดง่าย (คนหน้างานไม่ใช่ engineer), แก้น้อยที่สุด, ข้ามแพลตฟอร์ม
**ไม่แตะ:** `--thresh 4.0`, `--hold 5.0` (อยู่นอก scope), logic reference ของ absolute mode (ของรอบ 6)

## การเปลี่ยนแปลง (รายข้อตาม acceptance)

### A1 — ยึดสล็อต + ทนดวงหาย (`multiwatch.py`)
- เพิ่ม `detect_blobs()` (คืน blob 'ทุก' อันที่ผ่านเกณฑ์), refactor `find_spots()` ให้ใช้มัน
- `MultiWatch` เพิ่ม state `self.slots` + `self.slot_tol`; เมธอด `_lock_slots()` / `_assign()`
- พอเจอครบ N ครั้งแรก (หรือตอน `calibrate`) → จำตำแหน่ง x ของช่อง; เฟรมถัดไปจับ blob เข้าช่อง
  ที่ใกล้สุด (ภายใน tol = ครึ่งของช่องว่างที่แคบสุด, blob ใหญ่เลือกก่อน)
- ช่องที่ไม่มี blob = `state:"MISSING"` (รีเซ็ต timer + ล้าง window ช่องนั้น) — **ไม่ error ทั้งจอ**
- ยังไม่เคยเจอครบ → คืน `error: "เจอ X/N ดวง — จัดวางให้ครบ 1 ครั้งก่อน"`
- `update()` คืน `spots` ครบ n เสมอหลังยึดสล็อต → **map ดวง↔ชิ้นไม่เลื่อน** แม้บางดวงหาย
- `annotate()` วาดช่อง MISSING เป็นกรอบเทา + "?" (ไม่หายจากภาพ)

### A2 — detection ผ่อนปรน (`multiwatch.py`)
- `DETECT_REL` 0.85 → **0.6** (ครอบดวงหรี่กว่าได้) + `--rel` ปรับได้จาก CLI
- **เงื่อนไข:** ปลอดภัยเพราะ A3 ล็อกแสงต่ำ → พื้นหลังดำ (ถ้าใช้ auto-exposure ห้องสว่าง
  ค่า 0.6 อาจรวมแผงเป็นก้อน — ดูข้อ "ขอ reviewer ดู" #2)

### A3 — ล็อกแสงข้ามแพลตฟอร์ม + auto-lock (`webapp.py`)
- แยก `set_exposure(value, cap)` เป็นชั้น: `_use_uvc()` → mac+uvc-util (เดิม, ไม่เปลี่ยนพฤติกรรม) /
  ไม่งั้น `_set_exposure_opencv()` ใช้ `CAP_PROP_AUTO_EXPOSURE`/`EXPOSURE`/`WB_TEMPERATURE` best-effort
- ตั้งไม่ได้ → ไม่ crash คืนข้อความชัด "ใช้ auto แล้วจัดห้องให้มืดลงแทน"
- **เส้นทาง OpenCV ตั้ง property ใน capture thread เท่านั้น** (ผ่านคิว `exp_pending`/`exp_event`)
  กัน race กับ `cap.read()` — ฝั่ง mac/uvc เป็น subprocess เรียกตรงได้
- **auto-lock:** `--exposure` default = 5 (เปิดมาเห็นแต่ดวงไฟบนพื้นดำเลย), `--exposure 0` = ปล่อย auto

### D10 — camera picker เห็นภาพ (`webapp.py`)
- `GET /api/cameras` หยุด capture ชั่วคราว → ไล่เปิด OpenCV index 0–5 อ่าน 1 เฟรม ส่ง thumbnail base64 → เปิดกล้องคืน
- `POST /api/select_camera` ตั้ง `CAM_INDEX` + จำลง `config.json` + รีสตาร์ท capture
- UI: ปุ่ม "📷 เลือกกล้อง" โชว์ภาพเล็ก คลิกเลือก (ไม่ต้องรู้ index) + รีโหลด stream
- `--cam` default = None → ใช้ค่าที่จำใน `config.json` (ครั้งหน้าใช้กล้องเดิม)

### D11 — ปล่อยกล้องตอน idle (`webapp.py`)
- `stream()` อัปเดต `SES.last_stream` ทุกเฟรมที่ถูกดึง
- capture loop: ถ้า **ไม่ได้ตรวจ + ไม่มีคนดู stream เกิน 60 วิ** → `break` ปล่อยกล้อง
- `ensure_camera()`/`stream()` เปิดคืนอัตโนมัติเมื่อมีคนขอ — หน้างานที่ดูตลอด/ตรวจอยู่ ไม่ถูกปล่อย
- `open_cap()` ใหม่: เปิดกล้องแบบ **ไม่ `sys.exit`** (เดิม `open_camera` ทำ thread ตาย) เปิดไม่ได้ตั้ง `cam_error` ให้ UI แจ้ง

### D12 — เอกสาร (ไฟล์ใหม่)
- `README_USE.md` (ไทย, คนทั่วไป): ลำดับใช้งาน + ติดตั้ง/รันแยก mac/Win/Linux + แก้ปัญหา
- `requirements.txt` (เวอร์ชันขั้นต่ำที่ทดสอบ), `run_windows.bat` (ดับเบิลคลิกเปิดบน Windows)

## การทดสอบ (offline — กล้องจริงเป็นงานหน้างานตามข้อจำกัด)
- **engine (T1–T6 ผ่านหมด):** เจอครบ→ยึดสล็อต OK; บัง 1 ดวง→ช่องนั้น MISSING ดวงอื่นวัดได้ map ไม่เลื่อน
  (ดวง4 ยัง i=4); ฟ้า→BAD ถูกช่อง (relative median); A2 rel=0.6 เจอ 4/4 ส่วน 0.85 เจอ 3/4;
  absolute จับทั้งฟ้า+เหลือง; calibrate มีชิ้นเพี้ยนปน→ปฏิเสธ
- **webapp offline:** `py_compile` ผ่าน, import ผ่าน, `_use_uvc()`=True ที่เครื่องนี้,
  OpenCV no-cap ไม่ crash, config round-trip, status preview, start validation (total<2/>MAX→400),
  calibrate-before-start→400, clear/stop OK
- **CLI:** `multiwatch.py run --help` ผ่าน (มี `--rel`)
- **ค้างเทสหน้างาน (ต้องมีกล้อง):** `/stream`, `/api/cameras` (D10), idle release (D11),
  เส้นทาง OpenCV exposure บน Windows/Linux, และเทียบสีจริงในที่มืด + ยืนยันทิศ `--flip` (ค้างเดิม)

## ขอ reviewer ดูเป็นพิเศษ
1. **D11 idle:** ใช้ `last_stream` จาก `/stream` เป็นสัญญาณ "มีคนดู" — poll `/api/status` ไม่นับ
   (ตั้งใจ เพื่อให้แท็บที่เปิดทิ้งแต่ stream หลุดยังปล่อยได้) เหมาะสมไหม หรือควรนับ poll ด้วย
2. **A2 rel 0.85→0.6:** พึ่งสมมติฐาน "ล็อกแสง→พื้นดำ" ถ้าผู้ใช้กดแสงอัตโนมัติในห้องสว่าง
   อาจถอยกลับไปเจอปัญหาแผงรวมก้อน (บทเรียนรอบ 1) — ควรผูก rel กับ "ล็อกแสงอยู่ไหม" หรือปล่อยตามนี้
3. **เปลี่ยน default:** `--exposure` 5 (เดิม None), `--cam` ใช้ config (เดิม 0) — โอเคไหม
4. **ไม่ได้ทำ M1/M2/M3 จากที่เคยมีใน backlog** (exposure-change invalidates ref / engine race /
   per-spot ref) — อยู่นอก scope รอบนี้ตาม REVIEW รอบ 3 จงใจไม่แตะ ถ้าต้องการให้ยกเป็นรอบถัดไป

---

# HANDOFF — รอบที่ 6 (โหมดเทียบขาวอ้างอิง + นาฬิกาจับเวลา)

**วันที่:** 2026-06-12 (ค่ำ)
**ที่มา:** ผู้ใช้พบที่หน้างานว่า relative/median กลับทิศ — "ชิ้นฟ้าแต่ออกเหลือง"
เพราะในแถวมีชิ้นฟ้าหลายชิ้น median ลอยตามของเสีย (สมมติฐาน "ส่วนใหญ่ดี" พัง)
ผู้ใช้เสนอ "ไม่ใช่ขาว = เพี้ยน" → ตรงกับโหมด calibrate ขาวอ้างอิงที่สเปกเดิมเผื่อไว้
และตอนนี้**ทำได้แล้ว**เพราะล็อกทั้ง exposure (รอบ 5) และ **WB** (ทดสอบแล้ว: ตั้ง 5000K
ค้าง ไม่โดน auto ดึงกลับ) → ค่า b* เป็น absolute ที่เชื่อถือได้

## การเปลี่ยนแปลง

### multiwatch.py — absolute mode
- `MultiWatch(reference=...)` + `calibrate(frame)`: วางชิ้นดีครบ n จุด → ref = median b*
- มี ref: `dev = b* − ref`, NG เมื่อ `|dev| > thresh` (จับทั้ง**ฟ้าและเหลือง**) ค้าง 5 วิเดิม
- ไม่มี ref: relative เดิมทุกประการ (backward compatible)
- status เพิ่ม `mode` + `ref`, ทิศใน dev บอกฟ้า (ลบ) / เหลือง (บวก)

### webapp.py
- `set_exposure` ล็อก **WB 4500K** คู่กับแสงเสมอ (จำเป็นกับ absolute) / auto คืน default
- `POST /api/calibrate` (ต้องกำลังตรวจ — ใช้เฟรมดิบล่าสุด), `/api/calibrate/clear`
- reference **จำถาวร**ใน `calibration.json` — ข้าม start/stop และ restart แอป
- UI: ปุ่ม "🎯 Calibrate ขาว" / "ล้างอ้างอิง", แถบบอกโหมด (เตือนถ้ายังไม่ calibrate),
  ผลตาราง NG บอกทิศ "(ฟ้า)"/"(เหลือง)"
- **นาฬิกาจับเวลา** (คำขอผู้ใช้): panel ล่าง นาที+วินาที นับถอยหลัง ครบแล้ว
  แบนเนอร์แดงกะพริบ + เสียงติ๊ด 6 ครั้ง (WebAudio) + เปลี่ยน title แท็บ — JS ล้วน ไม่แตะ backend

## การทดสอบ (offline — กล้องปิดตามคำขอผู้ใช้เรื่องแบต)

- engine 5 เคสผ่านหมด: calibrate ปกติ / จับฟ้า+เหลืองถูกดวงถูกทิศ /
  **ฟ้าทั้งแถวจับได้หมด (จุดอ่อนเดิมหาย)** / ไม่ calibrate = relative เดิม / จุดไม่ครบ error ชัด
- persistence calibration.json ผ่าน / py_compile ผ่าน
- **ค้างเทสกล้องจริง**: ลำดับใช้งานจริง (ล็อกแสง → วางชิ้นดี → start → calibrate →
  สลับชิ้นเทส) + เช็คว่า WB 4500 เหมาะกับหน้างานไหม — รอผู้ใช้เปิดกล้องรอบหน้า

## ขอ reviewer ดูเป็นพิเศษ

1. ตรรกะ calibrate ใช้ median ของชิ้นดีทั้งหมดที่วาง — สมเหตุสมผลไหม หรือควรเก็บ per-spot
   (เผื่อ vignette ของเลนส์ทำให้จุดริมเฟรมค่าต่างจากกลางเฟรม)
2. ผูก ref กับ exposure ปัจจุบันไหม — ตอนนี้ถ้าผู้ใช้เปลี่ยนค่าแสงหลัง calibrate ref จะผิด
   โดยไม่มีเตือน (ผมยังไม่ได้ทำ guard — ควรเพิ่มไหม?)
3. thread-safety ของ calibrate (อ่าน frame copy ใต้ lock แล้วประมวลนอก lock)

---

# HANDOFF — รอบที่ 5 (หน้างานจริง: แก้แสงฟุ้งด้วยการล็อก exposure)

**วันที่:** 2026-06-12 (เย็น)
**ที่มา:** เทสหน้างานจริงครั้งแรก — LED สว่างจัดบนฉากมืด ทำให้ auto-exposure
เร่งแสงจนฟุ้งรวมเป็นปื้นขาว (อิ่มตัว 12.3% ของเฟรม ก้อนเดียวกว้าง 67% ของภาพ)
แยก blob ไม่ได้และไม่มีข้อมูลสีเหลือ — ซอฟต์แวร์อย่างเดียวแก้ไม่ได้

## การวินิจฉัย (มีหลักฐานทุกขั้น)

1. OpenCV/AVFoundation: `cap.set()` คืน False ทุก property — คุมไม่ได้ (ตรงตามสเปกเตือน)
2. `uvcc` (libusb): LIBUSB_TRANSFER_STALL ทุกคำสั่ง ทั้งตอน stream เปิดและปิด
3. `uvc-util` (IOKit — ตัวที่สเปกแนะนำ): **ใช้ได้** — ผู้ใช้ clone+build เอง
   (Claude Code ไม่อนุญาตให้ agent ดึงโค้ดภายนอกมารันเอง) binary เก็บที่ `tools/uvc-util`

## ผลจูน exposure (วัดจากภาพจริงทุกค่า)

| exposure | อิ่มตัว | แยก blob ได้ | วัดสีได้ |
|---|---|---|---|
| 156 (auto เดิม) | 10.4% | 6 (ปื้นยักษ์ 90k px) | – |
| 20 | 4.1% | 3 (halo ยังชนกัน) | 3 |
| 10 | 1.6% | 3 | 3 |
| **5** | **0.3%** | **10 จุดแยกสวย** | **10/10 จุด (786–4017 px/จุด)** |

→ **ใช้ exposure=5 (manual mode)** ที่ระยะกล้อง/แสงหน้างานปัจจุบัน
ค่า b* ที่วัดได้จริง 10 จุด: −0.1 ถึง −5.2 (บางชิ้นน่าสงสัยฟ้า — รอเทสตรวจจริง)

## สิ่งที่แก้ใน webapp.py

- `set_exposure(value|None)`: เรียก `tools/uvc-util -V 0x291a:0x3369` (เลือกกล้องด้วย
  vendor:product ไม่ใช่ index — กัน iPhone/กล้องอื่นเลื่อน index) / None = คืน auto
- `POST /api/exposure` + UI: ช่อง "☀️ แสง" + ปุ่ม "ล็อกแสง"/"แสงอัตโนมัติ" ในหน้าเว็บ
- `--exposure N` ตอนสตาร์ท — ตั้งหลังกล้องเปิด (พิสูจน์แล้ว uvc-util คุมได้ระหว่าง stream)
- ทดสอบ: สตาร์ทด้วย --exposure 5 → อ่านกลับจากกล้องได้ 5/mode=1 จริง, API เปลี่ยน 8→5 live ได้

## หมายเหตุ/ความเสี่ยงให้ reviewer ดู

- ค่า exposure=5 ผูกกับระยะกล้อง+ความสว่าง LED ปัจจุบัน — เปลี่ยนหน้างานต้องจูนใหม่ผ่าน UI ได้เอง
- จุดไฟจริงเรียงเฉียงเล็กน้อยตามมุมกล้อง — `find_spots` sort ตาม x ยังถูกลำดับ แต่ถ้ากล้องหมุน
  มากกว่านี้อาจต้อง cluster แถว
- แถวจริงมี ~10+ ดวง ชน MAX_SPOTS=10 พอดี — ถ้าจะตรวจมากกว่านี้ต้องคุยเรื่องลิมิต/resolution
- เกณฑ์ thresh/hold/smooth ไม่ถูกแตะเช่นเดิม

---

# HANDOFF — รอบที่ 4 (ฟีเจอร์ใหม่: ตรวจ N จุด + web app)

**วันที่:** 2026-06-12
**ที่มา:** ผู้ใช้ขอรองรับหลายชิ้นงานพร้อมกัน (วาง**แถวแนวนอน** — ผู้ใช้ระบุว่ายังไม่แน่ใจ
การจัดวางในอนาคต เอาแนวนอนก่อน) + UI ที่ user-friendly กรอกจำนวนแล้วตรวจตามนั้น
ผู้ใช้ยืนยันทิศซ้าย/ขวาแล้วด้วยตา: **ตรง ไม่ต้อง --flip** (ปิดข้อ 3 ของสเปกเดิม)

## ไฟล์ใหม่ (colorwatch.py ที่ผ่าน review ไม่ถูกแตะเลย)

| ไฟล์ | บทบาท |
|---|---|
| `multiwatch.py` | engine ตรวจ N จุด (2–10) — `find_spots` + class `MultiWatch` (ป้อนเฟรม→สถานะ ไม่ผูก UI) + CLI `run --spots N` |
| `webapp.py` | FastAPI: หน้าเว็บไทย กรอกชิ้น×ดวง / MJPEG stream วาดกรอบ / ตารางสถานะรายชิ้น / จำกัด MAX_SPOTS=10 |

deps เพิ่ม: fastapi, uvicorn (ใน .venv)

## ตรรกะตัดสินที่ขยาย (ขอ reviewer ดูเป็นพิเศษ)

- **N≥3: เทียบ median ของทุกจุด** — `dev_i = b*_i − median(ทั้งหมด)` แล้วใช้เกณฑ์เดิม
  (dev < −4 ค้าง 5 วิ) ข้อดี: แก้จุดอ่อน "เพี้ยนพร้อมกันสองฝั่งตรวจไม่ได้" ของ N=2 ไปในตัว
- **N=2: คงสูตรเดิมเป๊ะ** (`b_i − b_อีกฝั่ง`) — จงใจไม่ใช้ median เพราะตอน N=2 median อยู่กึ่งกลาง
  ทำ dev เหลือครึ่ง = เกณฑ์ 4.0 ไวขึ้น 2 เท่าโดยไม่ตั้งใจ
- ยืนยันด้วยเทสต์: จุดเพี้ยนเดียวกัน dev N=2 = dev N=10 = −12.79 (สเกลเดียวกัน)
- เกณฑ์ thresh 4.0 / hold 5.0 / smooth 15: ใช้ค่าเดิม ไม่แตะ

## บั๊กที่เจอเองระหว่างทดสอบกล้องจริง (แก้แล้ว)

`_reset_history` ตอนแรกรีเซ็ต hold timer ด้วยตอน re-detect ROI ทุก 10 วิ → สถานะ BAD
หลุดเป็น OK เป็นจังหวะ (เห็นจาก JSON จริง) — แก้ให้ล้างเฉพาะ rolling window ตาม semantics
ของ colorwatch แล้วยืนยัน: BAD ขึ้นที่ ~5 วิ แล้วค้างข้าม re-detect 2 รอบ (25 วิ)

## การทดสอบ

- เฟรมจำลอง 5 เคส: เจอครบ 2/4/5/10 จุดเรียงถูก / N=10 เพี้ยน 2 ดวง (ดวง 3,7) จับถูกดวง /
  สเกล dev N=2 = N=10 / hold ไม่ฟันธงก่อน 5 วิ / MAX_SPOTS reject
- กล้องจริง (แผง 2 จุด): `multiwatch run --spots 2` ให้ผลสอดคล้อง colorwatch เดิม
- web app จริง: `/api/start` 1×2 → ชิ้น 1 ดวงซ้าย BAD ถูกต้อง / เกินลิมิต (6×2=12) →
  ข้อความแนะนำแบ่งรอบ / MJPEG stream วาดกรอบแดง-เขียวถูกฝั่ง / หน้าเว็บโหลดปกติ
- ความปลอดภัย: default bind 127.0.0.1 เท่านั้น (LAN ต้องสั่ง --host 0.0.0.0 เองพร้อมรับความเสี่ยง
  — โดน auto-mode classifier ของ Claude Code เตือนเรื่องนี้มา เห็นด้วยจึงเปลี่ยน default)

## เพิ่มเติมรอบ 4.1: โหมดพรีวิว (ผู้ใช้ขอ "จบที่เดียว ใช้ง่ายสุด")

ปรับ `webapp.py`: กล้องเปิดค้างใน thread เดียวตลอดชีวิตแอป —
- เปิดหน้าเว็บ = เห็นภาพสดทันที (ป้าย PREVIEW) ใช้จัดวางชิ้นงานก่อนตรวจ ไม่ต้องรันอะไรแยกอีก
- กดเริ่มตรวจ = สร้าง engine สลับเป็นโหมดตรวจ (กรอบ+ตาราง) / กดหยุด = กลับพรีวิว กล้องไม่ดับ
- เปลี่ยน engine ภายใต้ lock เดียวกับ capture thread
- ทดสอบครบวงจรผ่าน: preview → start (1×2, ซ้าย BAD ถูก) → stop → กลับ preview
- ป้ายบนภาพเป็นอังกฤษ (OpenCV วาดไทยไม่ได้) คำอธิบายไทยอยู่ในหน้าเว็บแทน

## เพิ่มเติมรอบ 4.2: ป้าย NG/OK (คำขอจากหัวหน้างานของผู้ใช้)

เปลี่ยนภาษา UI เป็นมาตรฐาน QC ให้คนหน้าไลน์ที่ไม่รู้เทคนิคอ่านได้ทันที:
- แบนเนอร์ผลรวมตัวใหญ่: ✅ **OK ผ่านทุกชิ้น** (เขียว) / ❌ **NG ไม่ผ่าน: ชิ้นที่ X** (แดง กะพริบ)
- ตาราง: คอลัมน์ผลเป็น "NG ไม่ผ่าน" แดง / "OK ผ่าน" เขียว, ชิ้นที่ก็มี NG/OK กำกับ
- ป้ายบนกรอบในภาพ: `#1 NG -6.2` / `#2 OK -0.7` (multiwatch.annotate)
ทดสอบจริงแล้ว: เฟรมจากกล้องแสดง NG แดงฝั่งซ้าย OK เขียวฝั่งขวา ถูกต้อง

## คำขอต่อ reviewer

1. ตรวจตรรกะ median (N≥3) ว่าเห็นด้วยกับการคง N=2 เป็นสูตรเดิมไหม
2. ตรวจ thread-safety ของ `webapp.py` (capture thread ถือกล้องถาวร + สลับ engine ใต้ lock)
3. ข้อจำกัดที่รู้: ทดสอบจริงได้แค่ N=2 (แผงจริงมี 2 ดวง) — N>2 พิสูจน์ด้วยเฟรมจำลองเท่านั้น
   จนกว่าผู้ใช้จะมีของจริงหลายชิ้น

---

# HANDOFF — รอบที่ 3 (ตรวจรับ REVIEW.md รอบ 2)

**วันที่:** 2026-06-12
**สถานะ: review รอบ 2 = ผ่าน / implementer ตรวจรับ fix ของ reviewer แล้ว — ไม่มีข้อโต้แย้ง**

## ตรวจรับ 2 จุดที่ reviewer แก้โดยตรง

1. **วงรีหลุดใน relax branch ของ `measure_bstar`** — ยอมรับว่าเป็นบั๊กของ implementer จริง
   (รอบ 2 ใส่วงรีเฉพาะ branch หลัก ลืม AND ซ้ำตอนผ่อนเกณฑ์เหลือ 40 — ซึ่งเป็นเคสมืด
   = เคสใช้งานจริงพอดี ขอบคุณที่จับได้) วิธี factor `inside` ออกมาแล้ว AND ทั้งสอง branch ถูกต้อง
2. **pump `--show` ใน branch หาไฟไม่เจอ** — สอดคล้องกับที่แก้ B1 ไว้ ถูกต้อง

## ผลตรวจรับ (รันซ้ำฝั่ง implementer ทั้งหมดผ่าน)

- เฟรมจริง `raw_cam0.png`: diff = −8.672 ตรง baseline เดิมเป๊ะ (branch หลักไม่ถูกกระทบ)
- เคสมืด relax branch: แต้มสีนอกวงรี → b* ไม่ขยับเลย (วงรีทำงานแล้วใน branch นี้)
- `estimate_cct` ปกติ (L~7905K / R~6901K)
- กล้องจริง 12 วิ: diff −5.5 นิ่ง, ซ้าย BAD, cct_k ครบทุกรายงาน
- `py_compile` ผ่านทั้ง colorwatch.py และ snapshot.py

## คงเหลือ (ตรงกันทั้งสองฝ่าย — เป็นงานผู้ใช้)

1. ทดสอบสภาพมืดจริง (`snapshot.py --cam 0` ในที่มืด + ดู diff ตอนไฟปกติ)
2. ~~ยืนยันทิศซ้าย/ขวาด้วยตา~~ ✅ **ผู้ใช้ยืนยันแล้ว (2026-06-12): ทิศตรง ไม่ต้อง `--flip`**

## ทิศทางใหม่จากผู้ใช้ (รอ implementer เสนอแบบ → จะเริ่มหลังตกลง)

ผู้ใช้ต้องการรองรับ**หลายชิ้นงานพร้อมกัน** (เช่น 5 ชิ้น / 10 ดวง) + หน้า UI ที่ user-friendly
ให้กรอกจำนวนแล้วกล้องเช็คตามนั้น — implementer กำลังร่างข้อเสนอ (web app local + median reference)

---

# HANDOFF — รอบที่ 2 (ตอบ REVIEW.md รอบแรก) — เก็บเป็นประวัติ

**วันที่:** 2026-06-12
**งาน:** แก้ตามผลตรวจ — ตอบครบทุกข้อ ไม่มีข้าม

## ตอบ review ทีละข้อ

### B1 (บั๊กเงียบเมื่อวัดไม่ได้) — ✅ แก้แล้ว
`colorwatch.py` (ใน `cmd_run`): เมื่อ `measure_bstar` คืน `None` ตอนนี้
- พิมพ์ error ทุก `interval` ทั้งโหมดปกติและ JSON:
  `{"t":..., "error":"pixel ไม่พอ วัดค่าไม่ได้", "n_left":..., "n_right":...}`
- โหมด `--show`: วาดข้อความ "MEASURE FAILED" + ยังเรียก `waitKey` → หน้าต่างไม่ค้าง กด q ได้
- รีเซ็ต `HoldTimer.since` ทั้งสองฝั่ง → ช่วงไม่มีข้อมูลไม่ถูกนับเวลา hold (ปิดประเด็น R2 ส่วนที่สองด้วย)
- ทดสอบ: เฟรมมืดสนิท → คืน None ไม่ crash / เฟรมสลัว max=45 → เกณฑ์ผ่อน 40 ยังวัดได้

### R1 (ขอบไม้ bias ฝั่งขวา) — ✅ พิสูจน์แล้วว่าจริง + แก้แล้ว
พิสูจน์ด้วยการวัด `raw_cam0.png` 5 แบบ (box/ellipse × pad 0.35/0.15/0.05):
- b* ฝั่งขวาเปลี่ยนจาก **+0.71 (box ปัจจุบัน) → −3.0 (ตัดขอบหมด)** → ขอบไม้ bias จริงตามที่ reviewer สงสัย
- แต่ **diff อยู่ช่วง −8.2 ถึง −8.7 ทุกวิธี** (สเปรด 0.5) — ต่ำกว่าเกณฑ์ 4.0 มาก คำตัดสินไม่เคยพลิก
- **วิธีแก้ที่เลือก:** ellipse mask inscribed ในกรอบ (ใน `measure_bstar`) — ตัดมุมกรอบที่กินขอบแผง
  แต่คง pad 0.35 ไว้ให้ pixel เยอะพอสำหรับที่มืด (เลือกข้อ (ก)+(ข) ผสม แทนข้อ (ค) ที่ต้องใช้มือบังจริง)
- ผลวัดจริงหลังแก้: R เปลี่ยน +0.7 → −1.7 (bias หาย), diff = −5.48 — สมเหตุสมผลขึ้น

### R2 (ล้าง state ไม่ครบ) — ✅ แก้ทั้งสองจุด
- `diffs.clear()` เมื่อ re-detect แล้ว ROI เปลี่ยนตำแหน่ง
- `HoldTimer.since = None` ช่วงวัดค่าไม่ได้ (รวมอยู่ใน B1)

### R3 (ไม่มี sanity check blob) — ✅ แก้แล้ว
`find_two_spots`: centroid สอง blob ต้องห่างกันแกน x ≥ 60% ของความกว้าง blob ที่ใหญ่กว่า
ไม่งั้นคืน `None` — ทดสอบกับเคสจำลองเหตุการณ์ threshold 0.6 เดิม (แผงรวมก้อน + แถบสะท้อนด้านบน)
→ reject ถูกต้อง / เฟรมจริง + เคสสองจุดแยกชัด → ยังผ่านปกติ

### เอกสารไม่ตรงโค้ด — ✅ แก้แล้ว
อัพเดท `COLORWATCH_PROMPT.md` บรรทัด pipeline ข้อ 1 และ "สิ่งที่ต้องทำต่อ" ข้อ 2
ให้เป็น 85% พร้อมวงเล็บบอกที่มาการจูน

### ข้อ 2 ของสเปก (ทดสอบแสงจริงที่มืด) — ⏳ ยังทำไม่ได้ ไม่ใช่ข้าม
**เหตุผล:** ต้องการสภาพแวดล้อมมืดจริง ซึ่งควบคุมจากโต๊ะทำงานตอนนี้ไม่ได้ (ห้องเป็น office สว่าง)
สิ่งที่เตรียมไว้ให้ทดสอบเร็วเมื่อถึงสภาพจริง:
- `snapshot.py --cam 0` เก็บหลักฐาน ROI + ค่า b* ได้ใน 1 คำสั่ง
- เส้นทาง failure (B1) มี output ชัดเจนแล้ว ถ้ามืดเกินจะเห็น `"pixel ไม่พอ"` ทันที ไม่เงียบ
- threshold เป็น relative (85% ของ max ของเฟรมนั้นเอง) + floor 40 → ที่มืด max จะต่ำลง เกณฑ์ scale ตาม
**ขอให้ผู้ใช้กำหนดเวลา/สถานที่ทดสอบสภาพมืด** — เป็น blocker เดียวที่เหลือของข้อนี้

### ข้อ 3 ของสเปก (ทิศซ้าย/ขวา) — ⏳ รอผู้ใช้
ต้องการตามนุษย์ดูแผงจริงเทียบกับ output — ผมเปิดหน้าต่าง `--show` ให้แล้วรอบหนึ่ง
ผู้ใช้ยังไม่ได้ตอบผล รันซ้ำได้ด้วย:
`./.venv/bin/python colorwatch.py run --cam 0 --show` (ฝั่งที่กรอบแดง = ฝั่งที่ระบบว่าเพี้ยน)

## การทดสอบรอบนี้

- Unit offline: dark frame / dim frame (เกณฑ์ผ่อน 40) / sanity check 3 เคส / เทียบ ellipse กับผลทดลอง — ผ่านหมด
- End-to-end กล้องจริง 30 วิ (28 รายงาน, 0 error): **diff เฉลี่ย −5.48, SD 0.240** (min −5.95, max −5.08)
  ซ้ายขึ้น BAD หลังค้างครบ 5 วิ ถูกต้อง — ภาพ ROI ล่าสุดดู `snap_cam0.png`
- หมายเหตุ: diff เปลี่ยนจาก −8.5 (รอบก่อน) → −5.5 เพราะตัด bias ขอบไม้ออก — ทิศทางสอดคล้องกับ
  ที่ reviewer ทำนายไว้ว่า bias ดึง diff ติดลบเกินจริง

## ไฟล์ที่แตะรอบนี้

| ไฟล์ | การเปลี่ยนแปลง |
|---|---|
| `colorwatch.py` | 4 จุด: ellipse mask ใน `measure_bstar` / error path เมื่อวัดไม่ได้ / `diffs.clear()` เมื่อ ROI ขยับ / sanity check ใน `find_two_spots` |
| `COLORWATCH_PROMPT.md` | อัพเดทตัวเลข threshold 60%→85% สองจุด พร้อมที่มา |

เกณฑ์ `--thresh 4.0` / `--hold 5.0` / `--smooth 15` ยังไม่ถูกแตะเช่นเดิม

**สถานะ: พร้อมรับ review รอบถัดไป** — blocker ที่เหลือ: ทดสอบสภาพมืด (รอสภาพแวดล้อม) + ยืนยันทิศ (รอผู้ใช้)

---

# ภาคผนวกรอบที่ 2.1 — ทดสอบจำลองสภาพมืด (synthetic)

ผู้ใช้สั่งให้เดินหน้าเองไม่ต้องรอ จึงทำสิ่งที่ใกล้เคียงข้อ 2 ที่สุดเท่าที่ทำได้จากตรงนี้:
**จำลองความมืด**โดยเอา `raw_cam0.png` มา scale ความสว่างลง + ใส่ Gaussian noise เลียน gain สูง
(ย้ำ: **ไม่ใช่ตัวแทนของจริง** — กล้องจริงมี auto-exposure จะดัน gain ให้จุดไฟยังสว่างในเฟรม
การจำลองนี้จึง pessimistic กว่าความเป็นจริง / ผลทดสอบจริงยังจำเป็นอยู่)

## ผล (scale ความสว่าง × noise SD)

| สภาพ | gray_max | หา blob | วัดได้ | diff |
|---|---|---|---|---|
| 0.80 / sd2 | 207 | ✅ | ✅ | −5.89 |
| 0.50 / sd4 | 136 | ✅ | ✅ | −3.63 |
| 0.30 / sd6 | 92 | ✅ | ✅ | −1.95 |
| 0.18 / sd8 | 67 | ❌ (noise แตก mask เป็นเศษ <MIN_AREA) | – | – |
| ≤0.10 | ≤51 | ❌ | – | – |

## ข้อค้นพบสำคัญ (ให้ reviewer ชั่งน้ำหนัก)

1. **diff หดตามความมืด**: −5.9 → −3.6 → −2.0 — chroma ใน Lab ลดตามความสว่าง
   ถ้ากล้องจริง expose ต่ำตอนมืด จุดที่ "ฟ้าเท่าเดิม" อาจให้ diff ต่ำกว่าเกณฑ์ 4.0 → false negative
   นี่เป็นความเสี่ยงหลักที่ต้องเช็คตอนทดสอบจริง (ไม่ใช่เรื่องหา blob ไม่เจอ)
2. **failure mode ที่มืดจัดคือ MIN_BLOB_AREA + noise fragmentation** ไม่ใช่ threshold —
   ลองแล้ว Gaussian blur ก่อน threshold ก็ไม่กู้ (ต่ำกว่า max~50 หาไม่เจอทุกแบบ)
   จึง**ไม่แก้โค้ด**เพื่อสภาพที่อาจไม่เกิดจริง (auto-exposure น่าจะกันไว้)
3. ทุกเคสที่พัง ระบบ fail แบบมีเสียง (error ทุก interval) ไม่เงียบ — เส้นทาง B1 ที่แก้ไปทำงานครบ

---

# ภาคผนวกรอบที่ 2.2 — เทียบ b* vs องศาเคลวิน (CCT) + เพิ่มฟิลด์ cct_k

ผู้ใช้ถามว่าใช้เคลวินแทน b* ง่ายกว่าไหม → ทดลองเทียบจริง:

## ผลเทียบ (กล้องจริง 40 เฟรมต่อเนื่อง, วัดจาก usable mask เดียวกัน)

| ตัวชี้วัด | mean (L−R) | SD | **SNR** |
|---|---|---|---|
| b* diff | −5.75 | 0.022 | **265** |
| CCT diff (McCamy) | +748 K | 12.2 K | **61** |

→ **b* นิ่งกว่า ~4 เท่า** + ไม่เป็นเชิงเส้น/สูตร CCT ใช้ได้เฉพาะใกล้ขาว + เกณฑ์ 4.0 เดิม
calibrate มาในหน่วย b* → **คงใช้ b* ตัดสินเหมือนเดิม** (ผู้ใช้เห็นชอบแล้ว: "โอเคตามนั้น")

## สิ่งที่เพิ่ม (ผู้ใช้อนุมัติ)

`colorwatch.py`: ฟังก์ชันใหม่ `estimate_cct()` (sRGB→XYZ→xy→McCamy) + ฟิลด์ `cct_k`
ใน output ทั้ง JSON และโหมดปกติ — **ข้อมูลประกอบเท่านั้น ไม่แตะตรรกะตัดสิน**
คำนวณเฉพาะตอนรายงาน (1/วิ) ไม่เปลืองทุกเฟรม / แยกฟังก์ชันต่างหาก ไม่แตะ `measure_bstar`
ทดสอบจริง: `{"left":{"b":-8.39,"state":"BAD","cct_k":7847},"right":{"b":-2.25,"state":"OK","cct_k":7021},...}`

## โบนัส: ผลรันยาว 5 นาที (288 รายงาน)

diff: mean −5.98, SD 0.321, ช่วง [−6.66, −5.26] — **0 error** ข้าม auto-exposure cycle
สถานะ: BAD/OK 283 รายงาน (ซ้ายเพี้ยนค้าง), OK/OK 5 รายงานแรก (ช่วง hold ยังไม่ครบ 5 วิ — ถูกต้อง)

## เรื่องทิศซ้าย/ขวา (ข้อ 3)

ทำแทนผู้ใช้ไม่ได้ (ต้องเทียบกับแผงจริงด้วยตา) — ข้อสังเกตเชิงเทคนิค: OpenCV/AVFoundation
ไม่ mirror ภาพ webcam (ต่างจาก preview ของ FaceTime) ถ้าผู้ใช้มองแผงจากฝั่งเดียวกับกล้อง
label ควรตรงโดยไม่ต้อง `--flip` — ใช้เป็นสมมติฐาน default จนกว่าผู้ใช้ยืนยัน
