# งาน: ระบบตรวจสีไฟ LED 2 จุด (ซ้าย/ขวา) ผ่านกล้อง Anker PowerConf C200

## บริบท

- มีแผง diffuser ที่มีไฟ LED ส่อง 2 จุด (ซ้าย / ขวา) ติดตั้งตำแหน่งคงที่ ไม่ขยับ
- ต้องการรู้แค่อย่างเดียว: **ฝั่งไหน "เพี้ยน" (ออกฟ้า) ฝั่งไหนปกติ (ขาว)**
- กล้อง: Anker PowerConf C200 บน macOS — มี auto-exposure / auto-white-balance ที่**ปิดไม่ได้ง่ายๆ** ผ่าน OpenCV (AVFoundation ไม่รองรับ `CAP_PROP_EXPOSURE` กับกล้องนี้)
- สภาพแสงจริงตอนใช้งาน **มืดกว่าภาพทดสอบมาก** — ภาพที่เคยทดสอบเปิดไฟสว่างเพื่อให้ดูง่ายเท่านั้น
- เกณฑ์ตัดสินที่ใช้อยู่เดิม (จากงานก่อนหน้า): ค่า `db` (delta b* ใน Lab) **ติดลบเกิน 4 และค้างต่อเนื่อง 5 วินาที = ฟ้าจริง**

## แนวทางที่เลือกแล้ว (อย่าเปลี่ยนโดยไม่จำเป็น)

**เทียบซ้าย-ขวากันเองในเฟรมเดียวกัน (differential) แทนการ calibrate ค่า absolute**

เหตุผล: กล้อง auto-adjust ตลอดเวลา ค่า absolute จะลอย แต่สองจุดอยู่ในเฟรมเดียวกัน
โดน exposure/WB เดียวกัน → `diff = b*_left − b*_right` นิ่งกว่ามาก

ขั้นตอนใน pipeline:
1. หา blob สว่างสุด 2 อันด้วย threshold แบบ relative (85% ของ max — เดิม 60% แต่ตอนห้องสว่างทั้งแผงรวมเป็น blob เดียว, จูน 2026-06-12) + `connectedComponentsWithStats` → sort ตามแกน x ได้ left/right, cache ROI ไว้ re-detect ทุก 10 วิ
2. วัดสีที่ **halo รอบ core** — ตัด pixel อิ่มตัว (channel ≥ 250) ทิ้ง เพราะ core ขาวโพลนวัดสีไม่ได้ ใช้เฉพาะ pixel ที่ max channel อยู่ช่วง 80–249 (ผ่อนลงเหลือ 40 ถ้า pixel ไม่พอ)
3. แปลงเป็น Lab (OpenCV เก็บ b* แบบ offset +128 ต้องลบออก) → mean b* ต่อฝั่ง
4. `diff = b*_left − b*_right` ผ่าน rolling average 15 เฟรม
5. `diff < −4` ค้าง ≥ 5 วิ → ซ้ายเพี้ยน / `diff > +4` ค้าง ≥ 5 วิ → ขวาเพี้ยน
6. มี `--flip` สลับ label ได้ (เผื่อกล้อง mirror — ผู้ใช้จะลองแล้ว flip เอาถ้ากลับด้าน)

## สถานะปัจจุบัน

มีไฟล์ `colorwatch.py` ใช้งานได้แล้ว — ผ่านทั้งเฟรมจำลอง (ฟ้าซ้าย/ขาวขวา diff = −13.5)
และ**กล้องจริง cam 0** (2026-06-12: diff นิ่ง −5.1 ถึง −6.0, hold 5 วิ trigger ตรงเวลา,
ซ้ายถูกฟันธง BAD สอดคล้อง CCT ซ้าย ~7800K เย็นกว่าขวา ~7000K) โครงสร้าง:

```
python colorwatch.py list                  # หา camera index
python colorwatch.py run --cam 0 --show    # debug window วาด ROI + ค่า b*
python colorwatch.py run --json            # JSON lines: {"t","left":{"b","state","cct_k"},"right":{...},"diff"}
python colorwatch.py run --flip            # สลับ label
# ออปชัน: --thresh 4.0 --hold 5.0 --smooth 15 --interval 1.0
```

หมายเหตุ: `cct_k` (เคลวิน, สูตร McCamy) เป็นข้อมูลประกอบเท่านั้น การตัดสินเพี้ยน/ปกติ
ใช้ b* อย่างเดียว; การวัด b* จำกัดด้วยวงรี inscribed ในกรอบ กันขอบแผง/ขอบไม้ปนเข้าการวัด

deps: `opencv-python`, `numpy`

## สิ่งที่ต้องทำต่อ (เรียงตามลำดับ)

1. **ทดสอบกับกล้องจริง**: รัน `list` หา index ของ C200 แล้วรัน `run --show`
   เช็คว่ากรอบ ROI ครอบจุดไฟพอดีทั้งสองจุด
2. **จูนกับแสงจริง (มืดกว่าภาพทดสอบมาก)**:
   - ถ้าหา blob ไม่เจอ → ลด threshold ใน `find_two_spots` (ตอนนี้ 85% ของ max, floor 40 — จูนจาก 60% เมื่อ 2026-06-12 เพราะห้องสว่างทำแผงรวมเป็น blob เดียว)
   - ถ้าค่า diff แกว่งเพราะ noise จาก gain สูง → เพิ่ม `--smooth` หรือ `--thresh`
   - ห้ามแก้เกณฑ์ hold 5 วิ โดยไม่ถามผู้ใช้ — เป็นเกณฑ์ที่ตกลงไว้แล้ว
3. **เช็คทิศทาง**: ให้ผู้ใช้ดูว่า "ซ้าย" ใน output ตรงกับซ้ายจริงไหม ถ้าไม่ใช้ `--flip`
4. (ถ้าผู้ใช้ขอ) ต่อยอด: exit code / webhook / log ลงไฟล์ เมื่อสถานะเปลี่ยน OK↔BAD

## ข้อจำกัดที่รู้อยู่แล้ว (อย่าเสียเวลาแก้เอง)

- วิธี differential ตรวจไม่ได้ถ้า**ทั้งสองฝั่งฟ้าพร้อมกัน** (diff ≈ 0) — ยอมรับแล้ว
  ถ้าจำเป็นค่อยเพิ่มโหมด calibrate ขาวอ้างอิงทีหลัง
- ถ้าต้องล็อกกล้องจริงๆ บน macOS ให้ใช้ tool ภายนอกเช่น `uvc-util` ก่อนเปิด stream
  แต่แนวทางหลักคือไม่ต้องล็อกเพราะใช้ differential แล้ว

## กติกาการทำงาน

- ตอบ/อธิบายเป็นภาษาไทย โค้ดและ comment สำคัญเป็นไทยได้
- แก้ทีละจุด รันทดสอบก่อนแก้จุดถัดไป อย่า refactor ใหญ่โดยไม่ถาม
- ทุกครั้งที่จูนค่า ให้บอกผู้ใช้ว่าแก้ค่าไหน จากอะไรเป็นอะไร เพราะอะไร
